#!/bin/bash
# hash: {{ include "run_onchange_macos-apps.sh.tmpl" | sha256sum }}

# Maccy
defaults write org.p0deje.Maccy historySize -int 200
defaults write org.p0deje.Maccy pasteByDefault -bool true
defaults write org.p0deje.Maccy showInStatusBar -bool true
defaults write org.p0deje.Maccy KeyboardShortcut "option+command+v"

echo "macOS app defaults configured."
