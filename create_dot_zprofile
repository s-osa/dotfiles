# This file is created once by chezmoi and not overwritten afterward.
# External tools (e.g. Rancher Desktop, mise, volta) may append to this file freely.
#
# Add machine-specific settings directly below if needed.
# For settings that should be shared across machines, add them to ~/.zprofile.common instead.

source ~/.zprofile.common
